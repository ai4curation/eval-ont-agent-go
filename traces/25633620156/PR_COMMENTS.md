## Summary

Cleaned up the obsolete term stanza for `GO:0070221` (`obsolete sulfide oxidation, using sulfide:quinone oxidoreductase`) by removing the two remaining synonym tags from the obsolete term. The stanza now retains only the historical definition, obsoletion reason, term tracker link, `is_obsolete: true`, and `replaced_by: GO:0019418`.

## Rationale

The term was obsoleted because it represents a GO-CAM-style process term. GO obsolete-term practice is to remove synonyms unless there is a specific reason to retain them. The direct replacement remains `GO:0019418` (`sulfide oxidation`).

## Validation

- Pre-validation: `robot convert -i go-edit.obo -f obo -o /tmp/go-edit.pre.obo` completed successfully before edits.
- Term checks: inspected `GO:0070221` and replacement `GO:0019418` with `obo-grep.pl`; `GO:0070221` is only present in its obsolete stanza.
- Automated validation: `cd src/ontology && make travis_build` passed.
- Annotation impact check: attempted `runoak -i amigo: associations GO:0070221`, but the local OAK installation failed with a dependency error (`Format.JSON` attribute error). Since the term was already obsolete with a direct replacement and this PR only removes obsolete-term synonyms, no annotation migration decision was needed for this cleanup.

## Checklist

- [x] PLAN: Issue context reviewed; intended cleanup of `GO:0070221` was clear.
- [x] PRE-VALIDATION: Current ontology converted successfully before changes.
- [x] TERM-SEARCH: Relevant GO terms checked (`GO:0070221`, `GO:0019418`).
- [x] DESIGN-PATTERNS: Obsolete-term metadata pattern followed via term-obsoletion guidance.
- [x] EDITS: Used `obo-checkout.pl` / `obo-checkin.pl`; edited only the checked-out term file.
- [x] RELATIONSHIPS: Obsolete term has no `is_a`, `relationship`, or `intersection_of` tags; `replaced_by` is retained.
- [x] SPECIALIZED-EDITS: Term obsoletion guidance applied.
- [x] METADATA: Obsolete-term metadata retained; no new-term metadata added.
- [x] AUTOMATED-VALIDATION: `make travis_build` passed.
- [x] REFERENCE-VALIDATION: N/A; no references introduced or changed.
- [x] CHANGES-COMMITTED: Committed local change to `src/ontology/go-edit.obo` only.

---
🤖 **Generated by pi agent**
- Runtime: `pi`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/go-ontology-agent-config@v9:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-go/actions/runs/25633620156)
