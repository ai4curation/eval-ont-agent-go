## Summary

This branch scopes the secondary sexual characteristic development terms to Metazoa as requested in issue geneontology/go-ontology#31051 / PR #32027:

- `GO:0045136`, `GO:0046543`, and `GO:0046544` use `, sensu Metazoa` labels.
- The original labels are retained as exact synonyms.
- Definitions now use mammal examples rather than human-only wording.
- `GO:0045136` has an `only_in_taxon` constraint for `NCBITaxon:33208` Metazoa.
- `GO:0042695` retains its label (`thelarche`) and now has an updated OBO parent-label comment for `GO:0046543`, keeping the local source text consistent with the renamed parent.

The substantive term and taxon-constraint edits were already present in the starting branch. The new local commit is a scoped cleanup to the `GO:0042695` `is_a` comment in `src/ontology/go-edit.obo`, made via `obo-checkout.pl` / `obo-checkin.pl`.

## Validation

- Pre-validation before the local cleanup: `make -C src/ontology travis_build` passed.
- Post-edit validation: `make -C src/ontology travis_build` passed.
- All reported SPARQL checks completed with 0 violations.
- ELK reasoning completed as part of `travis_build`.

Note: an optional `runoak -i ols:ncbitaxon info Metazoa` lookup could not run because the local OAK installation has a dependency error (`Format.JSON` missing). This did not affect ontology validation; `NCBITaxon:33208` Metazoa is already used throughout the taxon-constraint tables, including the requested row.

## Checklist

- [x] PLAN: Issue context and PR discussion summary reviewed; request is clear.
- [x] PRE-VALIDATION: Current ontology validated before the local cleanup.
- [x] RESEARCH: N/A; no new literature-backed definitions or PMIDs introduced.
- [x] TERM-SEARCH: Relevant GO stanzas inspected with `obo-grep.pl`.
- [x] DESIGN-PATTERNS: Checked; no new terms or logical definitions, and no `intersection_of` changes.
- [x] EDITS: Used checkout/checkin workflow for the local OBO source cleanup.
- [x] RELATIONSHIPS: No semantic relationship changes; updated one stale OBO label comment on an existing `is_a`.
- [x] SPECIALIZED-EDITS: Taxon-constraint handling checked; existing `only_in_taxon` Metazoa row is present for `GO:0045136`.
- [x] METADATA: Existing `term_tracker_item` links are present on the three renamed terms; no `created_by`/`creation_date` added because no new terms were created.
- [x] AUTOMATED-VALIDATION: `make -C src/ontology travis_build` passed after changes.
- [x] REFERENCE-VALIDATION: N/A; no new references introduced.
- [x] CHANGES-COMMITTED: Local commit created for the scoped `go-edit.obo` cleanup.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/go-ontology-agent-config@v9:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-go/actions/runs/25633570153)
