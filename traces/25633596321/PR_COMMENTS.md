## Summary

Implemented Option A for issue geneontology/go-ontology#31051, as represented by PR #32027:

- Renamed GO:0045136, GO:0046543, and GO:0046544 with `, sensu Metazoa` suffixes.
- Preserved the original labels as EXACT synonyms for lookup/backward compatibility.
- Softened the definitions from human-specific examples to mammalian examples.
- Added `term_tracker_item` metadata linking the affected terms to issue #31051.
- Added an `only_in_taxon` constraint for GO:0045136 to Metazoa (`NCBITaxon:33208`) in `src/taxon_constraints/only_in_taxon.tsv`; this scopes the child terms by inheritance.
- Left GO:0042695 `thelarche` unchanged, relying on inherited scope from its parent.

## Rationale

The `sensu Metazoa` suffix provides editorial disambiguation for these GO terms without asserting homology between metazoan and possible non-metazoan secondary sexual characteristics. A parent-level `only_in_taxon Metazoa` constraint is preferable to adding individual `never_in_taxon` constraints for non-metazoan groups, and it directly addresses the original request to avoid the prior narrow yeast-only exclusions.

## Validation

- `make travis_build` passed locally.
- All 20 reported SPARQL checks passed with 0 violations.
- ELK reasoning completed as part of `travis_build`.

Note: I attempted the taxon-constraint skill's recommended `runoak -i ols:ncbitaxon info NCBITaxon:33208` lookup, but the local OAK/LinkML installation failed on import with `AttributeError: type object 'Format' has no attribute 'JSON'`. The TSV entry was nevertheless locally verified in `src/taxon_constraints/only_in_taxon.tsv`.

## Checklist

- [x] PLAN: Imported issue/PR context reviewed and intent confirmed as Option A from issue #31051.
- [x] PRE-VALIDATION: Current tracked ontology state was inspected; requested target changes were already present in `HEAD`.
- [x] RESEARCH: N/A; no new biological claims or PMIDs were introduced beyond the issue discussion.
- [x] TERM-SEARCH: GO:0045136, GO:0046543, GO:0046544, and GO:0042695 inspected with `obo-grep.pl`.
- [x] DESIGN-PATTERNS: N/A; no new terms or logical definitions were added.
- [x] EDITS: Requested term edits are present in `src/ontology/go-edit.obo`; no further direct edit was needed in this local checkout.
- [x] RELATIONSHIPS: Child `is_a` parent labels reflect the renamed parent term; no relationship changes beyond the requested taxon constraint were needed.
- [x] SPECIALIZED-EDITS: Taxon-constraint workflow consulted; `only_in_taxon` entry for GO:0045136 to Metazoa verified.
- [x] METADATA: `term_tracker_item` metadata present on the three edited terms; no `created_by` or `creation_date` added because these are existing terms.
- [x] AUTOMATED-VALIDATION: `make travis_build` passed locally.
- [x] REFERENCE-VALIDATION: N/A; no new references were introduced.
- [x] CHANGES-COMMITTED: The requested changes are already committed in the local `HEAD` merge commit for PR #32027; no empty follow-up commit was created.

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/go-ontology-agent-config@v9:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-go/actions/runs/25633596321)
