# Summary

Added a new biological process term:

* `GO:7770074 protein O-linked glycosylation via N-acetylglucosamine`

The term is placed under `GO:0006493 protein O-linked glycosylation` and includes the requested exact synonyms:

* `protein O-linked GlcNAcylation`
* `protein O-linked-N-acetylglucosaminylation`

# Rationale

The issue requested a distinct BP term for O-GlcNAcylation so that Ogt, Eogt, and orthologs can be annotated more specifically than the generic parent `protein O-linked glycosylation`.

The new term follows the existing local pattern used for sister terms such as `protein O-linked glycosylation via glucose`, `via galactose`, `via arabinose`, and `via xylose`: an asserted subclass of `GO:0006493`, a text definition describing the initiating O-linked sugar, and no logical definition. No specific DOSDP pattern was found for this branch, so no `intersection_of` axioms were added.

PMID:35536957 was validated and supports the definition. The cached reference title is "The O-GlcNAc Modification." It describes O-GlcNAc as modification of serine or threonine hydroxyl groups on proteins by O-linked beta-N-acetylglucosamine.

# Validation

* Pre-validation: `make travis_build` passed before edits.
* Post-edit validation: `make travis_build` passed after adding the term.
* Reference validation: `linkml-reference-validator validate text-file -r 'SUPPORT: (\S+:\S+) "(.*)"' -t 2 -R 1 RESEARCH.md` passed.

# Checklist

* PLAN: issue context analyzed; request was clear.
* PRE-VALIDATION: current ontology validated before edits.
* RESEARCH: `RESEARCH.md` created; PMID:35536957 validated.
* TERM-SEARCH: GO parent and sister terms searched in `src/ontology/go-edit.obo`.
* DESIGN-PATTERNS: `DESIGN_PATTERNS.md` created; existing GO precedent checked; no `intersection_of` added.
* EDITS: new term created in `terms/` and checked into `src/ontology/go-edit.obo` with `obo-checkin.pl`.
* RELATIONSHIPS: asserted `is_a: GO:0006493`; no over-specified logical definition.
* SPECIALIZED-EDITS: chemical-entity skill consulted because the request names N-acetylglucosamine; no CHEBI axiom was added.
* METADATA: term tracker item, `created_by`, and `creation_date` added for the new term.
* AUTOMATED-VALIDATION: post-edit `make travis_build` passed.
* REFERENCE-VALIDATION: PMID support text validated.
* CHANGES-COMMITTED: committed locally as `59d5ecd Add protein O-linked GlcNAc glycosylation term`.

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/go-ontology-agent-config@v9:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-go/actions/runs/25976504889)
