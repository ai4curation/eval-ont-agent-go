Obsoleted `GO:7770028 glycoprotein cargo receptor activity` with direct replacement to `GO:0038024 cargo receptor activity`.

Rationale:
- The issue request was clear and supplied a direct replacement.
- The term represented an undesirable substrate-type classification axis for cargo receptors. Per the issue discussion, these functions should be organized by transport domain rather than by cargo chemistry, with more specific vesicle-related children used where appropriate.

Changes made:
- Renamed the term to `obsolete glycoprotein cargo receptor activity`.
- Preserved the original definition with the required `OBSOLETE.` prefix.
- Added an obsoletion comment explaining the reason.
- Added `is_obsolete: true`.
- Added `replaced_by: GO:0038024`.
- Added a `term_tracker_item` for issue `#31948`.
- Removed the active `is_a` assertion from the obsolete term.

Impact checks:
- `obo-grep.pl --noheader -r 'GO:7770028' src/ontology/go-edit.obo` did not reveal any other GO term stanzas referencing this term beyond the target stanza itself.
- `runoak -i amigo: associations GO:7770028` returned no direct annotation rows in this environment.

Validation:
- `cd src/ontology && make travis_build` could not complete because the local environment is missing `amm`, which the build invokes while generating filtered RHEA xrefs.
- Standalone `robot` validation could not be run because `robot` is not installed in this environment.
- I did inspect the final diff to confirm the change is limited to the `GO:7770028` stanza in `src/ontology/go-edit.obo`.

Checklist:
- [x] PLAN: The issue and comments were reviewed and the intent was clear.
- [x] PRE-VALIDATION: Attempted; blocked by missing local tool dependency (`amm`) rather than an ontology error.
- [ ] RESEARCH: N/A for this direct obsoletion request with no new references introduced.
- [x] TERM-SEARCH: Checked the target term and in-file usages with `obo-grep.pl`.
- [ ] DESIGN-PATTERNS: N/A for a straightforward obsoletion.
- [x] EDITS: Used `obo-checkout.pl` / `obo-checkin.pl` and edited the checked-out term file.
- [x] RELATIONSHIPS: Removed active logical placement from the obsolete term as required.
- [x] SPECIALIZED-EDITS: Used the `/term-obsoletion` workflow.
- [x] METADATA: Preserved historical provenance, added obsoletion metadata, and linked the current tracking issue.
- [ ] AUTOMATED-VALIDATION: Blocked by missing `amm` and `robot` binaries in the local environment.
- [x] REFERENCE-VALIDATION: No new references added.
- [x] CHANGES-COMMITTED: Committed only `src/ontology/go-edit.obo`.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/go-ontology-agent-config@v9:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-go/actions/runs/25949057951)
