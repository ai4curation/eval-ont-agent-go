Obsoleted `GO:0010381` (`peroxisome-chloroplast membrane tethering`) and set a direct replacement to `GO:7770065` (`peroxisome-chloroplast membrane tether activity`).

Summary of changes:
- Updated `GO:0010381` to `name: obsolete peroxisome-chloroplast membrane tethering`
- Preserved the historical definition with the required `OBSOLETE.` prefix
- Added an obsoletion comment explaining that the old term represented a molecular function
- Added `replaced_by: GO:7770065`
- Added the issue tracker link to `https://github.com/geneontology/go-ontology/issues/31877`
- Removed the obsolete term's synonym and asserted parent, following GO obsoletion practice
- Removed four `never_in_taxon` rows for `GO:0010381` from `src/taxon_constraints/never_in_taxon.tsv`
- Regenerated the derived taxon-constraint artifacts so the obsolete term no longer carries inherited taxon-constraint superclass axioms

Rationale:
- The issue and follow-up comments make clear that `GO:0010381` was modeling a molecular function as a biological process term.
- `GO:7770065` already exists in the ontology as the intended replacement term and was explicitly requested as the replacement in the issue discussion.
- Leaving the old taxon constraints in place caused `make travis_build` to fail the `obsolete-reference-violation` check because the obsolete class still had generated superclass axioms via the taxon-constraint import.

Annotation impact checked:
- AmiGO direct associations for `GO:0010381`: 1
- Annotation found: `AGI_LocusCode:AT2G26350` (`PEX10`), evidence `IMP`, reference `PMID:17215364`, source `TAIR`
- This matches the issue discussion that the annotation should be moved to the new MF term rather than blocking obsoletion here.

Validation performed:
- Ran `make travis_build` in `src/ontology`
- Initial run failed because obsolete `GO:0010381` was still referenced by generated taxon constraints
- Removed the source `never_in_taxon` rows, regenerated `imports/go_taxon_constraints.owl`, reran `make travis_build`, and the build passed

Checklist:
- [x] PLAN: analyzed issue context and intent is clear
- [x] PRE-VALIDATION: ran ontology validation relevant to the edited state
- [ ] RESEARCH: not needed beyond issue-supplied PMID and annotation lookup for this direct replacement obsoletion
- [x] TERM-SEARCH: checked `GO:0010381` and `GO:7770065` in `src/ontology/go-edit.obo`
- [ ] DESIGN-PATTERNS: N/A for this obsoletion-only change
- [x] EDITS: used `obo-checkout.pl` and `obo-checkin.pl` workflow
- [x] RELATIONSHIPS: removed logical placement from the obsolete term; no residual ontology references remain in the edited sources
- [x] SPECIALIZED-EDITS: used obsoletion workflow; also removed source taxon constraints that invalidated the obsolete class
- [x] METADATA: obsolete term metadata updated appropriately; no created_by/creation_date added to existing term
- [x] AUTOMATED-VALIDATION: `make travis_build` passes after taxon-constraint cleanup
- [x] REFERENCE-VALIDATION: preserved existing PMID `17215364`; no new reference IDs introduced
- [x] ACCURACY: replacement aligns with the issue discussion and existing replacement term
- [x] ISSUE-ALIGNMENT: changes match the requested obsoletion scope

Committed locally as:
- `a8678c8` `Obsolete GO:0010381 peroxisome-chloroplast membrane tethering`

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/go-ontology-agent-config@v9:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-go/actions/runs/25977442380)
