# Summary

Addressed issue #31978 by completing the requested carbohydrate process obsoletions and cleaning the affected obsolete stanzas in `src/ontology/go-edit.obo`.

Changes made:
- Obsoleted `GO:0046179 keto-D-gluconate biosynthetic process` with the issue tracker link and an obsoletion comment noting no annotations or cross-references.
- Removed synonyms and logical axioms from obsolete terms, following current GO obsoletion practice.
- Removed `replaced_by: GO:0046179` from `GO:0046180`, since `GO:0046179` is now obsolete and should not be the active replacement target.
- Kept `GO:0019584` with `consider: GO:0034194` and `consider: GO:0034195`, matching the split between D- and L-galactonate catabolic process terms.
- Kept `GO:0046181` with `replaced_by: GO:0019524`.
- Fixed the typo `grouing` -> `grouping` in the `GO:0019584` obsoletion comment.

# Rationale

`GO:0019584`, `GO:0046180`, and `GO:0046181` were already obsolete in the local ontology, but retained synonyms that obsolete terms should not keep. `GO:0046179` was still active and had a biosynthetic-process logical definition; the issue comment explicitly noted that this term has no annotations and no cross-references, so it was obsoleted with no replacement.

Because `GO:0046179` is now obsolete, `GO:0046180` cannot safely retain it as a `replaced_by` target. `GO:0046181` retains the requested active replacement `GO:0019524`.

# Validation

- Pre-change validation: `make -C src/ontology travis_build` passed.
- Post-change validation: `make -C src/ontology travis_build` passed.
- `git diff --check -- src/ontology/go-edit.obo` passed.

Annotation impact note: I attempted the required OAK/AmiGO direct annotation check with `runoak -i amigo: associations`, but the local `runoak` installation failed before executing the query due to a Python dependency mismatch: `AttributeError: type object 'Format' has no attribute 'JSON'`. I then checked GOlr directly:
- `GO:0019584`: 1 direct annotation, EcoCyc `LgoR`, IMP, `PMID:17088549`; should move to `GO:0034195` as requested.
- `GO:0046181`: 1 direct annotation, EcoCyc `GhrB`, IMP, `PMID:9811658`; should move to `GO:0019524`.
- `GO:0046180`: 0 direct annotations.
- `GO:0046179`: 0 direct annotations.

# Checklist

- [x] PLAN: Issue title, body, and comment reviewed; requested obsoletions and replacements identified.
- [x] PRE-VALIDATION: Current ontology validated before edits.
- [x] RESEARCH: N/A; no new biological references or PMIDs were introduced.
- [x] TERM-SEARCH: Existing GO terms and references checked with `obo-grep.pl`.
- [x] DESIGN-PATTERNS: Relevant catabolic and biosynthetic process patterns checked; obsolete terms retain no logical definitions.
- [x] EDITS: Terms were edited through `obo-checkout.pl` / `obo-checkin.pl`.
- [x] RELATIONSHIPS: Obsolete terms have no `is_a`, `relationship`, or `intersection_of` axioms; `GO:0046180` no longer points to an obsolete replacement.
- [x] SPECIALIZED-EDITS: Term obsoletion workflow applied. Chemical-entity concerns were considered; no CHEBI IDs or chemical definitions were changed.
- [x] METADATA: Obsolete terms include namespace, obsolete definition text, obsoletion comment, `is_obsolete: true`, and issue tracker link where applicable.
- [x] AUTOMATED-VALIDATION: `make -C src/ontology travis_build` passed after edits.
- [x] REFERENCE-VALIDATION: N/A; no new references were added.
- [x] CHANGES-COMMITTED: Local commit `be66096` contains only `src/ontology/go-edit.obo`.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/go-ontology-agent-config@v9:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-go/actions/runs/25633561794)
