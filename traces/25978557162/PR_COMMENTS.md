Summary:

This issue's core requested ontology content was already present in the imported local checkout:

- `GO:7770062 vesicle membrane tethering activity` existed with the requested definition provenance from `PMID:19887069` plus the broader trafficking review `PMID:19575650`.
- `GO:0140177 membrane-membrane adaptor activity` already had the requested definition update to include `vesicle` alongside protein, protein complex, and organelle.

I made one additional minimal cleanup edit to improve lexical consistency within this branch:

- Added exact synonym `vesicle membrane adaptor activity` to `GO:7770062`.

Rationale:

- The parent term is `membrane-membrane adaptor activity`, so adding the adaptor-form synonym makes the child term discoverable under both `tether` and `adaptor` wording.
- This matches existing precedent in the same area of the ontology, where sibling terms under `GO:0140177` use a mix of primary labels and synonyms involving both `adaptor` and `tether` language.
- No logical definition was added. I found no dedicated DOSDP in this checkout for this subfamily, and an asserted `is_a` plus text definition remains the appropriate minimal representation.

Validation and workflow notes:

- Pre-validation: ran GO SPARQL-QC checks on `src/ontology/go-edit.obo` before editing; all passed.
- Term search: checked `GO:0140177`, `GO:7770062`, and related tether/adaptor MF terms in `src/ontology/go-edit.obo`.
- Research: validated `PMID:19887069` and `PMID:19575650` with `linkml-reference-validator`; created `RESEARCH.md` with validated support text.
- Design patterns: reviewed ontology precedent and created `DESIGN_PATTERNS.md`; no dedicated pattern file was found in `src/patterns/` for this exact term family.
- Edit procedure: used `obo-checkout.pl` / `obo-checkin.pl` workflow on `GO:7770062`.
- Post-edit validation: reran GO SPARQL-QC checks; all passed.

Completed checklist:

- [x] PLAN: issue context reviewed and request interpreted
- [x] PRE-VALIDATION: ontology SPARQL checks passed before edits
- [x] RESEARCH: references validated and summarized in `RESEARCH.md`
- [x] TERM-SEARCH: relevant GO terms reviewed in `go-edit.obo`
- [x] DESIGN-PATTERNS: precedent reviewed; `DESIGN_PATTERNS.md` created
- [x] EDITS: used checkout/checkin workflow via `terms/GO_7770062.obo`
- [x] RELATIONSHIPS: no relationship changes required; existing `is_a` retained
- [x] SPECIALIZED-EDITS: N/A
- [x] METADATA: existing metadata on the new term retained unchanged
- [x] AUTOMATED-VALIDATION: SPARQL-QC passed after edits
- [x] REFERENCE-VALIDATION: PMIDs validated
- [x] CHANGES-COMMITTED: committed only `src/ontology/go-edit.obo`

Local commit:

- `11779ed` `Add adaptor synonym for vesicle membrane tethering activity`

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/go-ontology-agent-config@v9:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-go/actions/runs/25978557162)
