Updated `GO:0061852` to align its label, definition, and classification with the biology described in the issue.

Summary of changes

- Renamed `GO:0061852` from `retrograde transporter complex, Golgi to ER` to `retrograde cargo receptor complex, Golgi to ER`.
- Refined the definition from a generic transporter-complex description to a cargo receptor complex description.
- Changed the asserted parent from `GO:1990351 transporter complex` to `GO:0062137 cargo receptor complex`.
- Preserved the older transporter wording as `BROAD` synonyms:
  - `retrograde transporter complex, Golgi to endoplasmic reticulum`
  - `retrograde transporter complex, Golgi to ER`
- Left the existing `capable_of_part_of` relationship to retrograde Golgi-to-ER transport unchanged.

Rationale

- The issue requested that this term be treated as a cargo receptor complex rather than a generic transporter complex.
- The new primary label now matches the requested biological scope more precisely.
- The older transporter wording is broader than the revised concept, so keeping it as `BROAD` synonymy is safer than leaving it as exact synonymy.
- No new logical definition was added; a minimal parent/label/definition update was sufficient and lower risk.

Validation and workflow

- Read the issue context from `__issue_context__.json`.
- Consulted existing ontology precedent for `GO:0062137 cargo receptor complex` and the current `GO:0061852` stanza.
- Followed the GO edit workflow using `obo-checkout.pl` and `obo-checkin.pl` with a local `terms/` checkout file.
- Pre-validated by running `robot convert -i go-edit.obo -f obo`.
- Ran full ontology validation with `cd src/ontology && make travis_build` and it passed.

Checklist

- [x] PLAN: issue analyzed and requested change was clear
- [x] PRE-VALIDATION: syntax conversion succeeded before edits
- [x] TERM-SEARCH: inspected `GO:0061852` and `GO:0062137`
- [x] DESIGN-PATTERNS: confirmed no new logical pattern was needed; minimal structural edit only
- [x] EDITS: used checkout/checkin workflow rather than direct editing of `go-edit.obo`
- [x] RELATIONSHIPS: retained the existing transport relationship; updated only the parent needed by scope
- [x] METADATA: no new term metadata added because this was an edit to an existing term
- [x] AUTOMATED-VALIDATION: `make travis_build` passed
- [x] REFERENCE-VALIDATION: reused existing PMID-backed definition provenance; no new references introduced
- [x] ISSUE-ALIGNMENT: changes match the request for parent, label, and definition refinement

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/go-ontology-agent-config@v9:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-go/actions/runs/25980862588)
