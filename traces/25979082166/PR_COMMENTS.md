Obsoleted `GO:0009095` aromatic amino acid biosynthetic process, prephenate pathway.

Rationale:

- The term mapped to `MetaCyc:PWY-3481`, which is a superpathway combining the L-phenylalanine and L-tyrosine branches.
- GO already has the more appropriate separate biological process terms:
  - `GO:0009094` L-phenylalanine biosynthetic process
  - `GO:0006571` L-tyrosine biosynthetic process
- Keeping `GO:0009095` encourages over-composed pathway annotations and obscures which branch is actually supported.

Ontology edits made:

- Renamed the term to `obsolete aromatic amino acid biosynthetic process, prephenate pathway`.
- Replaced the definition text with the canonical `OBSOLETE.` form while preserving the original provenance.
- Removed synonyms, xref, asserted parentage, and logical axioms from the obsolete term.
- Added an obsoletion comment.
- Updated the `term_tracker_item` to issue `#32005`.
- Added `consider` tags for `GO:0009094` and `GO:0006571`.

Annotation impact review:

- `runoak -i amigo: associations GO:0009095` returned 7 direct annotations.
- PMID:21102469 supports phenylalanine biosynthesis specifically, so the Petunia and possibly the UniProt Arabidopsis annotations are candidates for migration to `GO:0009094`.
- PMID:20883697 supports PAT enzyme involvement in the prephenate-to-arogenate step, but from the abstract alone does not clearly support the combined obsolete term; manual review is warranted.
- PMID:18727669 supports chorismate mutase activity for `Rv0948c`, not the combined pathway term, so removal is the likely annotation action.
- Automated `IEA`/`ISS` annotations should be remapped or dropped in downstream annotation maintenance.

Validation:

- Pre-change validation: `make travis_build` passed.
- Post-change validation: `make travis_build` passed.

Research and pattern notes:

- Created local `RESEARCH.md` and `DESIGN_PATTERNS.md` notes to record annotation-impact analysis and the obsolete-term formatting pattern.
- PubMed pages for PMID validation were checked directly in this environment because `linkml-reference-validator cache reference` hit PubMed rate limiting (`HTTP 429`).

Checklist:

- [x] PLAN: analyzed issue context and obsoletion request
- [x] PRE-VALIDATION: ontology validated before edits
- [x] RESEARCH: annotation/publication impact reviewed and documented locally
- [x] TERM-SEARCH: checked `GO:0009095`, `GO:0009094`, and `GO:0006571`
- [x] DESIGN-PATTERNS: applied obsolete-term pattern guidance
- [x] EDITS: used `obo-checkout.pl` and `obo-checkin.pl`
- [x] RELATIONSHIPS: removed obsolete logical axioms and retained only obsoletion metadata
- [x] SPECIALIZED-EDITS: used obsoletion workflow
- [x] METADATA: preserved historical definition provenance and added correct issue tracker item
- [x] AUTOMATED-VALIDATION: `make travis_build` passed after edits
- [x] REFERENCE-VALIDATION: PMIDs checked directly via PubMed fetches; rate-limit issue noted
- [x] CHANGES-COMMITTED: ontology change committed locally

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/go-ontology-agent-config@v9:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-go/actions/runs/25979082166)
